@inherits EntityIdListBase<IDiscordUserInfo>

<div class="id-list d-flex flex-column">
    @foreach (ulong id in base.Values)
    {
        <UserIdListItem @key="id" Value="id" ValueChanged="base.OnValueChangedAsync" />
    }
    @if (this.AllowNewValues)
    {
        <UserIdListItem @ref="base.NewItemField" ValueChanged="base.OnNewValueInputAsync" />
    }
</div>

@code {
}
