@inherits EntityIdListBase<RoleInfoResponse>

<div class="id-list d-flex flex-column">
    @foreach (ulong id in base.Values)
    {
        <RoleIdListItem @key="id" Value="id" ReadOnly="base.ReadOnly" ValueChanged="base.OnValueChangedAsync" />
    }
    @if (this.AllowNewValues && !base.ReadOnly)
    {
        <RoleIdListItem @ref="base.NewItemField" ReadOnly="base.ReadOnly" ValueChanged="base.OnNewValueInputAsync" />
    }
</div>
