@inherits EntityIdListBase<RoleInfoResponse>

<div class="id-list d-flex flex-column">
    @foreach (ulong id in base.Values)
    {
        <RoleIdField @key="id" AllowNone="this.AllowNone" Value="id" ReadOnly="base.ReadOnly" ValueChanged="base.OnValueChangedAsync" />
    }
    @if (this.AllowNewValues && !base.ReadOnly)
    {
        <RoleIdField @ref="base.NewItemField" AllowNone="this.AllowNone" ReadOnly="base.ReadOnly" ValueChanged="base.OnNewValueInputAsync" />
    }
</div>

@code {
    [Parameter]
    public bool AllowNone { get; set; }
}